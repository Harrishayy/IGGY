
# a_x, a_y, a_z = accelerometer.read()

# motor_speed = 0

# while (distance > 100):
#     if (a_y < 1):
#         motor_speed = motor_speed + 1
#         motor.set_speed(motor_speed)

#     else:
#         motor_speed = motor_speed - 1
#         motor.set_speed(motor_speed)

#     distance = distance_to_ball()

# motor.set
